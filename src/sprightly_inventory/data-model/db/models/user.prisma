model User {
  id        String   @id
  email     String   @unique
  name      String?
  gender    String?
  bio       String?  @map("biography")
  role      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
